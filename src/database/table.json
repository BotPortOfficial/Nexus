{
  "Database": [
    "CREATE TABLE IF NOT EXISTS Users (UserId VARCHAR(20) PRIMARY KEY, Username VARCHAR(255) NOT NULL, Roles TEXT, JoinedServer DATETIME NOT NULL, CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP, UpdatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP)"
  ]
}